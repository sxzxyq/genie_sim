order:
  - waist.yaml
  - head_yaw.yaml
  - head_pitch.yaml
  - left_arm.yaml
  - right_arm.yaml

standalone_groups: []

groups:
  - name: Default
    includes:
      - waist.yaml
      - head_yaw.yaml
      - head_pitch.yaml
      - left_arm.yaml
      - right_arm.yaml

  - name: Head
    includes:
      - head_yaw.yaml
      - head_pitch.yaml

  - name: Left_Arm
    includes:
      - left_arm.yaml

  - name: Right_Arm
    includes:
      - right_arm.yaml

  - name: Dual_Arm
    includes:
      - left_arm.yaml
      - right_arm.yaml

  - name: Waist
    includes:
      - waist.yaml

  - name: Right_Arm_Waist
    includes:
      - waist.yaml
      - right_arm.yaml
